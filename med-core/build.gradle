
apply plugin: 'kotlin'

dependencies {
    api project(":med-common")
    implementation 'org.springframework.boot:spring-boot-starter-web'
    api "${lib."mybatisPlus"}"
//    implementation "${lib."druid"}"
    api 'mysql:mysql-connector-java'
}
